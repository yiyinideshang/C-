# 登录和退出

```mysql
mysql -u 用户名 -p
mysql -> quit
```

# 数据库-库与表

我们可以将数据库理解为一个图书馆，图书馆中存储了各式各样的书籍，不同类型的书籍都会放在自己 对应的区域上，对于书架上的书籍也会有不同属性的分类。

这里我们把**库看成图书馆**，**数据表就是一个一个的区域**，数据字段就是书架上书籍的不同的属性。

## 数据库-库

数据库中的 “库”，可以想象成一个大型的综合仓库。这个仓库用于有组织、有规则地存放和管理各种类 型的数据。

在 “库” 里，数据不是杂乱无章地堆积在一起，而是按照一定的数据结构和逻辑关系进行分类和存储。比 如，将有关用户信息的数据放在一起，将产品销售的数据放在另一个区域。

“库” 的存在使得数据的存储更加集中、规范和安全。它为数据提供了一个统一的管理环境，方便进行数 据的添加、删除、修改、查询等操作。同时，它还能够有效地控制数据的访问权限，确保只有授权的人 员能够对数据进行相应的操作，保护数据的隐私和完整性。

## 数据库-表

数据库中，“表” 是用于组织和存储数据的基本结构，就好比是一个规整的电子表格。

表由行和列组成。每一行代表一条完整的记录，类似于表格中的一行数据，包含了特定对象或事件的相 关信息。每一列则代表一种特定的属性或字段，比如姓名、年龄、地址等。

## 数据库-字段

“字段” 是构成数据库表中列的基本单元，用于定义和描述表中每一列所存储数据的特定属性或特征。

每个字段都有其明确的数据类型，比如整数、字符串、日期、布尔值等，这决定了该字段能够存储的数 据形式和范围。例如，一个 “年龄” 字段可能被定义为整数类型，以存储整数值；而 “姓名” 字段通常会被 定义为字符串类型，用于容纳文字字符。

字段还具有特定的约束条件，例如是否允许为空值、是否唯一等，以保证数据的完整性和准确性。

那么对于图书馆的图书我们要进行查找的时候，管理员会有一套他们自己的方式去找寻图书。对于数据 库而言也是如此，会有一套数据库的操作语句来操作数据库。

# 数据库语句

数据库语句是一种特定的语言表达式，它们遵循严格的语法规则，以准确地传达我们的操作意图。这些 语句如同数据库世界的语言，使我们能够对数据库中的数据进行创建、读取、更新和删除等操作

不同的数据库语句具有不同的功能和用途，但它们共同协作，使得数据库能够高效、准确地响应我们的 各种需求，实现对数据的有效管理和利用。

数据库的语法规则:

```mysql
数据库语句;
```

## 数据库-常规语句 

显示当前 `Mysql` 的版本

```mysql
SELECT VERSION();
```

显示当前的日期

```mysql
SELECT NOW();
```

显示当前用户

```mysql
SELECT USER();
```

**清空数据库终端的显示**(调用的是windows 的终端清空命令)

```mysql
mysql->system cls;
```

## 数据库-库操作语句

### 显示所有的库

```mysql
SHOW DATABASES;
```

### 创建一个库

```mysql
CREATE DATABASE 数据库名;
```

### 创建库的时候指定编码格式

```mysql
CREATE DATABASE 数据库名 character SET 编码;
```

### 修改库的编码格式

```mysql
ALTER DATABASE 数据库名 character SET 编码;
```

### 删除一个库

```mysql
DROP DATABASE 数据库名;
```

### 进入库

```mysql
USE 数据库名;
```

### 显示当前所在库

```mysql
SELECT DATABASE();
```

## 数据库-表操作语句

### 查看当前库中的所有表

```mysql
SHOW TABLES;
```

![屏幕截图 2025-11-18 221203](D:\Typora\typora_work\C++研修阶段\1数据库\2数据库语句1库与表images\屏幕截图 2025-11-18 221203.png)

### 创建一个数据表

```mysql
CREATE TABLE 表名(
    字段名1 字段类型1 约束条件,
    字段名2 字段类型2 约束条件...
);
```

#### 创建一个学生信息表：

![屏幕截图 2025-11-18 220950](D:\Typora\typora_work\C++研修阶段\1数据库\2数据库语句1库与表images\屏幕截图 2025-11-18 220950.png)

#### 创建一个学生成绩表：

![屏幕截图 2025-11-18 221424](D:\Typora\typora_work\C++研修阶段\1数据库\2数据库语句1库与表images\屏幕截图 2025-11-18 221424.png)

### 删除一个数据表

```mysql
DROP table 数据表名;
```

### 修改表

`ALTER` 是一个用于修改表结构和其他数据库对象定义的关键字

# 字段的类型：

**整数类型：**

-  `TINYINT` ：非常小的整数 `tinyint`
- `SMALLINT` ：较小的整数 `smallint`
- `MEDIUMINT` ：中等大小的整数 `mediumint`
- `INT` 或 `INTEGER` ：标准整数  `intger`
- `BIGINT` ：大整数 `bigint`

**字符串类型：** 

- `CHAR(size)` ：固定长度的字符串 `char`
- `VARCHAR(size)` ：可变长度的字符串 `varchar`
- `TINYTEXT` ：小文本字符串 `tinytext`
- `TEXT` ：文本字符串 `text`
- `MEDIUMTEXT` ：中等长度文本字符串 `mediumtext` 
- `LONGTEXT` ：长文本字符串 `longtext`

**浮点数和定点数类型：** 

- `FLOAT` ：单精度浮点数 

- `DOUBLE` ：双精度浮点数 

- `DECIMAL` 或 `NUMERIC` ：精确数值，适合用于货币等要求精确度的数据

  `decimal` `numeric`

**日期和时间类型** 

- `DATE` ：日期 
- `TIME` ：时间 
- `DATETIME` 或 `TIMESTMP` ：日期和时间组合
- `YEAR` ：年份

**二进制类型**：

-  `BINARY(size)` ：固定长度的二进制字符串  `binary`
- `VARBINARY(size)` ：可变长度的二进制字符串  `varbinary`
- `TINYBLOB` ：小二进制对象 `tinyblob`
- `BLOB` ：二进制对象  `blob`
- `MEDIUMBLOB` ：中等大小二进制对象 `mediumblob`
- `LONGBLOB` ：大二进制对象 `longblob`

**布尔类型**： 

- `BOOLEAN` 或 `BOOL` ：真/假逻辑变量

**枚举和集合类型（主要用于 `MySQL/MariaDB` ）：**

- `ENUM(value1,value2,…) `：枚举类型，只能有一个值，且必须是在列表中的一个
- `SET(value1,value2,…)` ：集合类型，可以有多达64个成员的集合，多个值之间用逗号隔

# 字段的约束条件:

## **非空约束**

- `not null` 表示该字段不能为空
- `null` 表示该字段可以为空

## **主键约束** 

- 每一个表里面都会存在一个**主键**，主键是这张表的唯一标识，可以用来标识这张表的所有数据
  - 主键不能重复（是**唯一的**） 
  - 主键不能为空（是**不为空的**)
- `primary key` 主键 
- `auto_increment` 自动编号

```c++
-- 方式1：在字段定义时指定
CREATE TABLE stu_infor (
    id INT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(20) NOT NULL
    ident varchar(18) not null unique key
);

-- 方式2：在表定义末尾指定
CREATE TABLE stu_infor (
    id INT AUTO_INCREMENT,
    name VARCHAR(20) NOT NULL,
    PRIMARY KEY (id)
);
```

## **唯一约束**

- `unique key` 标识该字段是唯一的。
- 唯一约束可以为空 
- 一张表下面**可以有多个唯一字段**

## **默认约束**

- `default` 创建字段时为默认值

## **外键约束** 

- 语法结构

  ```sql
  foreign key (子表的数据名) references 父表名(父表数据名)
  ```

- 建立在两张及以上的表的约束

- 外键列：加入了 foreign key 关键词的一列 

- 参照列：外键列参照的那一列 

- 子表：具有外键列的那个表 

- 父表：子表参照的表 

- 外键约束：保证约束的字段是唯一的。

```mysql
create table stu_score(
    id int not null,
    name varchar(20) not null,
    scores float not null,
    foreign key(id) references stu_infor(id)
);
```

# ==查看表结构==

两种查询语句  

- ```mysql
  show columns from 数据表名; 
  ```

- ```mysql
  desc 数据表名;
  ```

查看学生信息表和学生成绩表：

![屏幕截图 2025-11-18 221530](D:\Typora\typora_work\C++研修阶段\1数据库\2数据库语句1库与表images\屏幕截图 2025-11-18 221530.png)

| Key类型 | 含义       | 特点           |
| :------ | :--------- | :------------- |
| `PRI`   | 主键       | 唯一、非空     |
| `UNI`   | 唯一索引   | 唯一、允许空值 |
| `MUL`   | 非唯一索引 | 允许重复值     |
| 空      | 无索引     | 没有建立索引   |

`MUL` = `Multiple`（多值）表示该字段允许重复值 是一个普通的非唯一索引

`PRI` = `Primary Key`（主键）表中每条记录的唯一标识符 唯一且非空

# 作业：

- 创建学生信息表和学生成绩表 
  - 学号 主键 
  - 身份证 唯一 
  - 学号要是外键

# ==--------------------==

# 修改表

## 增加字段(增加列) `add`

```mysql
alter table 表名 add 字段名(列名) 类型(长度) 约束条件;
```

## 修改字段属性 `modify`

```mysql
alter table 表名 modify 字段名 类型 约束;
```

## 修改字段名 `change`

```mysql
alter table 表名 change 旧字段名 新字段名 类型 约束条件;
```

## 删除字段 `drop`

```mysql
alter table 表名 drop 字段名; 
```

## 创建约束条件 `add constraint`

```mysql
alter table 表名 add constraint 约束条件 字段名(列名);
```

## 修改表名 `to`

```mysql
rename table 旧表名 to 新表名;
```

## 修改表的编码 

```mysql
alter table 表名 character set 编码;
```

## 增加索引

```mysql
ALTER TABLE 表名 ADD INDEX 索引名(字段);
```



# 数据操作语句 

无非就是对数据库中数据表进行**增删改查**操作。

## 增加数据记录 `insert`

为某个数据表增加一条数据记录

关键字： `insert` 

```mysql
INSERT into 表名 (字段名1,字段名2...) values(值1,值2,...);
```

- 没有字段名列表，那么默认添加全字段。 
- 如果填写了字段列表，那么需要根据字段列表的说明进行数据插入。

```mysql
 insert into stu_infor(id,name,ident) values(1,'李四','130431xxxx1129xxxx');
```

## 查询数据记录   `select` 

### 单表查询

对某个表进行数据的查询操作 

关键字： `select` 

单表查询 

```mysql
SELECT 字段名1,字段名2... FROM 表名;
```

- 可以填写 * 来表示全部字段。 

  ```mysql
  select * from stu_infor;
  ```

- 根据需求在表名后面增加一些查询条件。 

#### Where筛选条件 

`Where` 用户查询数据的条件筛选。 

- `like` 模糊查询， `%` 代表多个字符， `_` 代表一个字符，

  - 例： `like '王%'`  表示开头为王的字符串 

    ````mysql
    SELECT * FROM students WHERE name LIKE '王%'; -- 查找姓王开头的名字
    ````

- `in` 在多个范围指定

  ```mysql
  SELECT * FROM students WHERE age IN (18, 20, 22); -- 查找年龄是 18、20 或 22的学生
  ```

- `between and` 区间范围

  ````mysql
  SELECT * FROM students WHERE age BETWEEN 18 AND 20; -- 查找年龄在 18 到 20 之间的学生
  ````

-  `or` 或者 A条件或者B条件满足一个即可

  ```mysql
  SELECT * FROM students WHERE age = 18 OR gender = '女'; -- 查找年龄是 18 或者性别为女的学生
  ```

-  `and` A条件和B条件同时满足的情况下

  ```mysql
  SELECT * FROM students WHERE age > 18 AND gender = '男'; -- 查找年龄大于 18 且性别为男的学生
  ```

#### 其他筛选条件

- `ordery` 排序 （根据字段进行排序 `asc` 正序（默认） `desc` 倒序）

  ```mysql
  SELECT * FROM students ORDER BY age ASC; -- 按照年龄升序排列
  SELECT * FROM students ORDER BY age DESC; -- 按照年龄降序排列
  ```

- `group by` 分组 （把每一个种类列举一条出来）

  ```mysql
  SELECT gender, COUNT(*) FROM students GROUP BY gender; -- 按照性别分组并统计数量
  ```

- `having` 分组筛选 （一般情况跟下`having`会和聚合函数连用）

  ```mysql
  SELECT gender, COUNT(*) FROM students GROUP BY gender HAVING COUNT(*) > 10; -- 按照性别分组，筛选出数量大于 10 的组
  ```

- `limit` 分页 `mysql`分页

  ```mysql
  SELECT * FROM students LIMIT 10; -- 显示前 10 条记录
  SELECT * FROM students LIMIT 5, 10; -- 从第 6 条开始，显示 10 条记录
  ```

  - `Limit n;`  `n` `pagesize` ------页显示几条数据 
  - `Limit m,n;`  `startrow` 起始记录 如果`m`为2，那么 `startrow` 记录为`m+1` = 3,显示`n`条记录

- `disTinct` 查询去重

  ```mysql
  SELECT DISTINCT class FROM students;
  ```

#### 别名使用

关键字： `as`

```mysql
SELECT 表名.字段名 as 别名(重命名) FROM 表名;
```

### 多表查询

显示两张表的交叉记录（两张表记录的全排列）

#### 内连接

关键字： `inner join on` 可省略

```mysql
select * from 表1,表2;
select * from 表1 inner join 表2 on 表1.字段名 = 表2.字段名;
```

````mysql
select * from stu_infor inner join stu_score on stu_infor.name=stu_score.name;
````

#### 外连接（可以定义主表）

-  左连接（ left join ）

- 右连接（ right join ）

```mysql
SELECT * FROM 表1 left join 表2 on 表1.字段 = 表2.字段;
```

- 以表1为主表，表2为副表 
- 如果表1的记录数多于表2，那么则以表1为主要显示的表，表2的记录数不够则用 null 进行填充

```mysql
 select * from stu_infor left join stu_score on stu_infor.id = stu_score.id;
```

### 子查询

可以在查询语句里面嵌套一个查询语句。

#### `where` 子查询

```mysql
SELECT * FROM 表名 where name=(SELECT);
```

```mysql
mysql> select * from stu_infor where name = (select name from stu_score where name = '李四');
mysql> select * from stu_infor where id = (select id from stu_score where name = '李四');
--查询成绩表中名字为李四的name,这个name学生信息
--查询成绩表中名字为李四的id，这个id的学生信息
```

#### `in` 子查询

````mysql
SELECT * FROM 表名 where 字段 in(SELECT);
````

```mysql
select * from stu_infor where id in (select id from stu_score);
+----+--------+--------------------+
| id | name   | ident              |
+----+--------+--------------------+
|  1 | 李四   | 130431xxxx1129xxxx |
|  2 | 王五   | 13043xxxxx112xxxxx |
+----+--------+--------------------+

查询学生信息表，当学生信息的id存在于学生成绩表中时
```

```mysql
select * from stu_score where id in (select id from stu_score);
+----+--------+--------+
| id | name   | scores |
+----+--------+--------+
|  1 | 李四   |   99.9 |
|  2 | 王五   |     98 |
+----+--------+--------+
2 rows in set (0.00 sec)
查询学生成绩表，当学生成绩表的id在学生信息表中时
```

```mysql
select * from stu_score where id in (1,2);
只查询成绩表的前两项
```

#### `from` 子查询

```mysql
SELECT * FROM (SELECT * FROM 表名)) as a;

SELECT 列名
FROM (SELECT 列名 FROM 表名 WHERE 条件) AS 别名
WHERE 条件;
```

```mysql
-- 查询每个部门的平均工资，然后找出高于公司平均工资的部门
SELECT dept_avg.*
FROM (
    SELECT department_id, AVG(salary) as avg_salary
    FROM employees
    GROUP BY department_id //按照部门分组
) AS dept_avg
WHERE dept_avg.avg_salary > (
    SELECT AVG(salary) FROM employees
);
```

## 更新数据 `update`

关键字： `update`

```mysql
update 表名 SET 字段名1=新值,字段名2=新值.... WHERE 条件;
```

## 删除数据 `delete`

关键字： `delete`

````mysql
delete from 表名;
````

按照需求在表后面增加条件.

# 作业：

自行操作数据操作语句。
