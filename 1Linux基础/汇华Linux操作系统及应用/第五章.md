# 文件、目录 5.2.1-5.2.3 ~5.2.4-5.2.6

# 5.2.1 显示文件内容

1. cat
2. more
3. less
4. head
5. tail
6. echo
7. od

## cat

功能：用来**串接文件或显示文件的内容**，也可以从**标准输入设备读取数据**并将结果重定向到一个新的文件中,达到建立新文件的目的。

| 选项          | 作用                                              |
| ------------- | ------------------------------------------------- |
| -n或-number   | 由1开始对所有输出的行数编号                       |
| -b            | 和-n相似,只不过对于空白行不编号                   |
| -s            | 当遇到由连续两行以上的空白行,就替换为一行的空白行 |
| -E--show-ends | 在每行结束处显示 $                                |

**输出重定向**：

````bash
cat a1.txt a2.txt>a3.txt#cat a1.txt a2.txt->a3.txt
cat a1.txt a2.txt>>a3.txt#cat a1.txt a2.txt->>a3.txt
````

- `>`：表示输出重定向，将a1和a2的内容输出到a3，如果a3.txt的不存在则创建一个新的a3.txt，如果存在则覆盖。
- `>>`：表示追加重定向：将a1和a2的内容输出到a3的尾部，如果a3.txt的不存在则创建一个新的a3.txt。

## more

## head

显示文件的前 n 行,默认显示文件的前10行。

选项：

| 选项 | 作用                                     |
| ---- | ---------------------------------------- |
| -n   | 显示文件的前 n 行,默认显示文件的前10行。 |
| -c   | 显示文件的前 n个字节。                   |

```bash
head -2 test1 #显示文件的前2行
head -n 2 test1 #显示文件的前2行
head -n -5 test1 #显示文件除后5行以外的所有内容
```

```bash
head -c 25 /etc/passwd #显示文件的前25个字节(不换行)
head -c -25 /etc/passwd #显示文件除后25个字节以外的所有内容(不换行)
```

## echo

输出字符串到基本输出,通常就是在显示器上输出。相当于C语言的`printf`。

# 5.2.2 文件的创建和删除

- touch
- rm

## touch

 **核心作用**

- **创建空文件**（最常用）。
- **更新文件的“修改时间”和“访问时间”**为当前时间。

| 选项 | 作用                                                     |
| ---- | -------------------------------------------------------- |
| -r   | 把指定文档或目录的日期时间统计成参考文档或目录的日期时间 |
| -d   | 以yyyymmdd的形式给出要修改的日期                         |
| -t   | 以yyyymmddhhmm.ss的形式给出要修改的日期和时间            |

```bash
#touch a.txt	#若 a.txt 不存在则创建空文件；若存在则更新时间戳。
touch file1 file2 file3	#一次创建 / 更新多个文件。
touch -r e1 e3 #将e3的文件时间更改为e1的文件时间
touch -d 20160502 a1 #修改文件a1的日期,以yyyymmdd的形式
touch -t 201809161020.32 a1
```

![屏幕截图 2026-04-13 095240](D:\Typora\typora_work\1Linux基础\汇华Linux操作系统及应用\屏幕截图 2026-04-13 095240.png)

![屏幕截图 2026-04-13 095333](D:\Typora\typora_work\1Linux基础\汇华Linux操作系统及应用\屏幕截图 2026-04-13 095333.png)

- 例3：

![屏幕截图 2026-04-13 095440](D:\Typora\typora_work\1Linux基础\汇华Linux操作系统及应用\屏幕截图 2026-04-13 095440.png)

- 例四：

![屏幕截图 2026-04-13 095608](D:\Typora\typora_work\1Linux基础\汇华Linux操作系统及应用\屏幕截图 2026-04-13 095608.png)

## rm

Linux rm（英文全拼：remove）命令用于删除一个文件或者目录。

- **语法**

```bash
rm [options] name...
```

**参数**：

- **-i 删除前逐一询问确认。**
- -f 即使原档案属性设为唯读，亦直接删除，无需逐一确认。
- **-r 将目录及以下之档案亦逐一删除。**

### 示例

删除文件可以直接使用rm命令，若删除目录则必须配合选项"-r"，例如：

```bash
# rm  test.txt 
rm：是否删除 一般文件 "test.txt"? y  
# rm  homework  
rm: 无法删除目录"homework": 是一个目录  
# rm  -r  homework  
rm：是否删除 目录 "homework"? y 
```

删除当前目录下的所有文件及目录，命令行为：

```bash
rm  -r  * 
```

文件一旦通过rm命令删除，则无法恢复，所以必须格外小心地使用该命令。

# 5.2.3 显示目录内容及更改命令

- ls
- pwd
- cd 

## `ls`

- 功能描述：列出目录的内容，是list的缩写
- 语法：`ls [选项][文件或目录]`

| 参数                        | 说明                                                         |
| --------------------------- | ------------------------------------------------------------ |
| `-a` 或 `--all`             | 显示所有文件（包括以 `.` 开头的隐藏文件）。                  |
| `-A` 或 `--almost-all`      | 显示除 `.` 和 `..` 外的所有文件（包括隐藏文件）。            |
| `-l`                        | 以长格式（详细信息）列出文件（权限、所有者、大小、修改时间等）。 |
| `-h` 或 `--human-readable`  | 与 `-l` 一起使用时，以人类可读的格式显示文件大小（如 KB、MB）。 |
| `-t`                        | 按修改时间排序（最新优先）。                                 |
| `-r` 或 `--reverse`         | 反向排序（配合 `-t`、`-S` 等使用）。                         |
| `-S`                        | 按文件大小排序（大文件优先）。                               |
| `-R` 或 `--recursive`       | 递归列出子目录内容。                                         |
| `-F` 或 `--classify`        | 在文件名后附加标识符（如 `/` 表示目录，`*` 表示可执行文件）。 |
| `--color`                   | 彩色输出（通常默认启用，`--color=auto`）。                   |
| `-i` 或 `--inode`           | 显示文件的 inode 编号。(**显示索引节点**)                    |
| `-n` 或 `--numeric-uid-gid` | 以数字形式显示 UID 和 GID（替代用户名和组名）。              |
| `-d` 或 `--directory`       | 仅显示目录本身，而非其内容（常用于配合 `-l`）。              |
| `-1`                        | 每行只显示一个文件（默认在终端宽度不足时自动启用）。         |
| `-m`                        | 以逗号分隔的列表形式显示文件。                               |
| `-Q` 或 `--quote-name`      | 用引号括住文件名（适用于含空格的文件名）。                   |
| `--group-directories-first` | 先显示目录，后显示文件。                                     |
| `--time-style=<格式>`       | 自定义时间显示格式（如 `+%Y-%m-%d`）。                       |

### ls -l

在使用 **ls -l** 命令时，第一列的字符表示文件或目录的类型和权限。其中第一个字符表示文件类型，例如：

- \- 表示普通文件
- d 表示目录
- l 表示符号链接
- c 表示字符设备文件
- b 表示块设备文件
- s 表示套接字文件
- p 表示管道文件

在使用 **ls -l** 命令时，第一列的其余 9 个字符表示文件或目录的访问权限，分别对应三个字符一组的 **rwx** 权限。例如：

- r 表示读取权限
- w 表示写入权限
- x 表示执行权限
- \- 表示没有对应权限

前三个字符表示所有者的权限，中间三个字符表示所属组的权限，后三个字符表示其他用户的权限。例如：

```bash
-rw-r--r-- 1 user group 4096 Feb 21 12:00 file.txt
```

表示文件名为file.txt的文件，**所有者**具有读写权限，**所属组**和**其他用户**只有读取权限。

查找最近修改的文件：

```bash
ls -lt | head -5
```

显示最近修改的 5 个文件。

统计文件数量：

```bash
ls | wc -l
```

统计当前目录下的文件数量(不包括隐藏文件)。

### ls -l常见颜色

ls 命令的输出颜色可以通过 --color 选项控制：

| 颜色                 | 文件类型                                       |
| :------------------- | :--------------------------------------------- |
| **蓝色**             | **目录**                                       |
| **绿色**             | **可执行文件**（有执行权限的普通文件）         |
| **浅蓝色**（青色）   | **符号链接**（软链接）                         |
| **红色**             | 压缩包（`.tar`、`.gz`、`.zip` 等）或损坏的链接 |
| **黄色**             | **设备文件**（如 `/dev/sda`）                  |
| 粉色/紫色            | 图片、音频、视频等多媒体文件（取决于配置）     |
| **白色/**灰色****    | 普通文件                                       |
| **红色背景**（闪烁） | 无效的链接（链接目标不存在）                   |
| **黑色背景+红色**    | setuid 文件（如 `passwd`）                     |

### 查看是内部命令还是外部命令2

`/bin`目录下显示的是Linux的命令，命令包括内部命令和外部命令，在`/bin`下显示文件名的是外部命令，没有显示的是内部命令

```bash
hanghaoboo@zhanghaoboo-virtual-machine:~$ cd /bin
zhanghaoboo@zhanghaoboo-virtual-machine:/bin$ ls cd
ls: 无法访问'cd': 没有那个文件或目录
zhanghaoboo@zhanghaoboo-virtual-machine:/bin$ ls pwd
pwd
zhanghaoboo@zhanghaoboo-virtual-machine:/bin$ ls ls
ls
zhanghaoboo@zhanghaoboo-virtual-machine:/bin$ ls touch
touch
zhanghaoboo@zhanghaoboo-virtual-machine:/bin$ cd 
zhanghaoboo@zhanghaoboo-virtual-machine:~$ pwd
/home/zhanghaoboo
zhanghaoboo@zhanghaoboo-virtual-machine:~$ 
```

### ls --full-time

**查看文件的完整时间信息**

```bash
zhanghaoboo@zhanghaoboo-virtual-machine:~$ ls --full-time
总用量 68
-rw-r--r-- 1 zhanghaoboo zhanghaoboo 8980 2026-03-02 10:55:49.491662767 +0800 examples.desktop
-rw-rw-r-- 1 zhanghaoboo zhanghaoboo    4 2026-04-13 10:40:34.124669381 +0800 hard_a2
-rw-rw-r-- 2 zhanghaoboo zhanghaoboo    0 2026-03-30 11:05:07.195507064 +0800 link.text
-rw-r--r-- 1 zhanghaoboo zhanghaoboo 5359 2026-03-16 10:57:45.238031777 +0800 manpath.config
drwxrwxr-x 2 zhanghaoboo zhanghaoboo 4096 2026-04-13 10:14:13.353388758 +0800 test1
-rw-rw-r-- 1 zhanghaoboo zhanghaoboo 5404 2026-03-16 10:54:53.088534786 +0800 test.config
-rw-rw-r-- 2 zhanghaoboo zhanghaoboo    0 2026-03-30 11:05:07.195507064 +0800 test.text
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 公共的
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 模板
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 视频
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 图片
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 文档
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 下载
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:01:09.731095782 +0800 音乐
drwxr-xr-x 2 zhanghaoboo zhanghaoboo 4096 2026-03-02 11:21:23.724769056 +0800 桌面
zhanghaoboo@zhanghaoboo-virtual-machine:~$ 

```

**linux文件的三种时间属性分别为：**

- **atime**，为access time（访问时间）的缩写，显示的是文件中的数据最后被访问的时间，读一次这个文件的内容，这个时间就会更新，比如被系统的进程直接使用或者通过一些命令和脚本间接使用。（执行一些可执行文件、脚本）或对这个文件运用 more、cat等命令。ls、stat命令都不会修改文件的访问时间

- **mtime**，为modify time（修改时间）的缩写，显示的是文件的内容被改变时的最后时间，比如用vi编辑时就会改变（也就是Block的内容），ls -l列出的时间就是这个时间。

- **ctime**，为change time（状态改动时间）的缩写，显示的是文件的权限，拥有者，所属的组，链接数发生改变时的时间。当然当内容改变时，也会改变。（也就是Inode内容发生改变和Block内容发生改变时），通过chmod、chown命令修改一次文件属性，这个时间就会更新。

### stat

`stat`是 `Linux` 下用于**显示文件或文件系统详细状态信息的命令**，能够输出 **inode 信息、权限、大小、时间戳、文件类型** 等，比 `ls -l` 提供更全面的数据。

## `pwd`

`print working directory`：用于显示**当前工作目录的绝对路径**。(完整路径)

- 在 Bash 中，`pwd` 通常是一个**内置命令**（shell builtin），同时系统中也有一个独立的可执行文件（如 `/bin/pwd`）。
- 可以通过 `type pwd` 查看：

```bash
type pwd
#pwd 是 shell 内建
```

**参数说明:**

| 选项        | 说明                                                   |
| :---------- | :----------------------------------------------------- |
| `-L`        | 显示**逻辑路径**（**默认行为**），**保留符号链接名称** |
| `-P`        | 显示**物理路径**，解析（展开）符号链接                 |
| `--help`    | 显示帮助信息                                           |
| `--version` | 显示版本信息                                           |

- 这里的解析符号链接指的是：**是否把路径中的符号链接（软链接）替换成它指向的真实路径**。

### pwd 命令的工作原理

`pwd` 命令通过以下方式确定当前工作目录：

1. 检查环境变量 `PWD` 的值（当使用 `-L` 选项时）
   - **逻辑路径**（`-L`）是 shell 维持的当前工作目录变量，反映了你是“怎么走进来”的。
2. 或者通过遍历父目录来物理确定路径（当使用 `-P` 选项时）
   - **物理路径**（`-P`）是通过系统调用获取的真实文件系统路径，去掉了所有符号链接的中间层。

多数情况下，`-L` 更符合你的直观感受（当进的是链接目录，自然希望显示链接名）。`-P` 则用于需要确切知道文件实际存放位置的场景，比如权限检查、挂载点判断等。

### 使用 -P 选项

```bash
$ ln -s /var/www/html myweb
$ cd myweb
$ pwd
/home/username/myweb    # 逻辑路径
$ pwd -P
/var/www/html          # 物理路径
```

## cd

`cd`（英文全拼：change directory）命令用于改变当前工作目录的命令，切换到指定的路径。

若目录名称省略(直接使用`cd`)，则进入使用者的 `home` 目录。

另外

- **~ 也表示为 home 目录** ， 
- **.** **表示目前所在的目录**，
- **..** **表示目前目录位置的上一层目录**。

```bash
cd [dirName]
```

- `dirName`：要切换的目标目录，可以是相对路径或绝对路径。

### 示例

切换到 /usr/bin/ 目录:

```bash
cd /usr/bin
#cd bin #当当前在usr目录下时,直接使用相对路径也可进入该目录下的bin目录
```

**切换到上级目录：**使用 **..** 表示上级目录，可以通过连续多次使用 **..** 来切换到更高级的目录。

```bash
cd ..        #回到上一级目录
cd ../../    #切换到上上级目录
```

**切换到用户主目录（home）：**使用 **~** 表示当前用户的主目录，可以使用 cd 命令直接切换到主目录。

```bash
cd ~
# cd
```

**切换到上次访问的目录：**使用 **cd -** 可以切换到上次访问的目录。

```bash
cd -
```

**切换到环境变量指定的目录：**可以使用环境变量来指定目标目录，并使用 **cd** 命令切换到该目录。

```bash
cd $VAR_NAME
```

# 5.2.4 目录的创建和删除

- mkdir
- rmdir

## `mkdir`

Linux mkdir（英文全拼：make directory）命令用于创建目录。

要在某个目录下创建文件或子目录，你需要对该目录拥有 **写 (`w`) 权限**。无论你是否在这个目录里，只要**没有写权限就无法创建**（可以**借助超级用户权限**，此时可以在命令前加上`sudo`，临时提权来完成操作。）；反之，有了写权限就能创建。

- **语法**

```bash
mkdir [-p/-m/-v] dirName
```

**参数说明**：

| 选项 | 作用                                               |
| ---- | -------------------------------------------------- |
| -p   | 依次创建目录，需要时创建目录的上级目录             |
| -m   | 设置权限模式，在创建目录时按模式指定设置目录权限。 |
| -v   | 每次创建新目录时，都显示执行过程信息。             |

- -p 确保目录名称存在，不存在的就建一个。
- -m 选项用来设置目录的权限。对目录的 **读**、**写**、**执行**权限分别为 **4、2 、1**，它们的和表示**目录所有者** 或 **所属组**中其他人对目录的权限 或 系统中**其他人**   对该目录的权限。
- -m的格式为`mkdir -m [参数] [目录名]`，这里的参数信息见后文

- 常见的参数组合有：

| 参数 | 含义                                           |
| ---- | ---------------------------------------------- |
| 600  | 只有所有者有读写权限                           |
| 700  | 只有所有者有读写以及执行的权限                 |
| 644  | 所有者有读写权限，组用户和其他用户只有读的权限 |
| 666  | 每个人都有可读可写权限                         |
| 777  | 每个人都有读和写以及执行的权限                 |

### 示例

```bash
mkdir runoob #在当前工作目录下，建立一个名为 runoob 的子目录
mkdir -p runoob2/test #在工作目录下的 runoob2 目录中，建立一个名为 test 的子目录。
#若 runoob2 目录原本不存在，则建立一个。（注：本例若不加 -p 参数，且原本 runoob2 目录不存在，则产生错误。）
mkdir -m 777 test4 #在当前工作目录下,创建test4目录，每个人都有读、写和执行的权限。	
```

## `rmdir`

Linux rmdir（英文全拼：remove directory）命令**删除空的目录**。

- **语法**

```bash
rmdir [-p/-v] dirName
```

**参数说明**：

| 选项 | 作用                                             |
| ---- | ------------------------------------------------ |
| -p   | 当子目录被删除后其父目录为空目录时，则一同删除。 |
| -v   | 显示详细的进行步骤                               |

### 示例

```bash
rmdir AAA #将工作目录下，名为 AAA 的子目录删除 :
rmdir -p BBB/Test #在工作目录下的 BBB 目录中，删除名为 Test 的子目录。若 Test 删除后，BBB 目录成为空目录，则 BBB 亦予删除。
rmdir -v test2 #删除目录test2并显示过程(test2不是空白目录不能删除)
rm -ri test2 #递归删除目录test2及其内部所有文件和子目录,并删除时询问
```

# 5.2.5 复制、移动命令

- cp
- mv





# 5.2.6 压缩、备份命令

